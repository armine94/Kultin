#include <stdio.h>
//Вычисление частного.
int main()
{float a;//bajaneli
float b;//bajanarar
printf("Вычисление частного.");
printf("Введите в одной строке делимое и делитель,");
printf("затем нажмите <Enter>.");
scanf("%f\t",&a);
scanf("%f\n",&a);
printf("->%f"a,b);
if(0==b)
{
prinrf("Вы ошиблись. Делитель не должен быть равен нулю.");
else 
printf("a/b= %f",a/b);
}
return 0; 
}
